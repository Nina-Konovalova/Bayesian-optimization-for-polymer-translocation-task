# Bayesian optimization experiments for landscape task
----------------------------------------------------

Make sure you have install all dependencies from requirements.txt installed before proceeding:

```
pip install requirements.txt
```
